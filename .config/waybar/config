{
  "layer": "top",
  "position": "top",
  "height": 32,
  "modules-left": ["custom/launcher", "hyprland/workspaces"],
  "modules-center": ["clock"],
  "modules-right": [
    "cpu", "memory", "temperature", "network", "pulseaudio", "battery", "tray"
  ],

  // Custom launcher
  "custom/launcher": {
    "format": "",
    "tooltip": false,
    "on-click": "rofi -show drun",
    "on-click-right": "wofi --show run",
    "exec-if": "which rofi",
    "interval": 3600
  },

  "clock": {
    "format": "󰅐 {:%A, %d %b  %H:%M}",
    "tooltip": true,
    "tooltip-format": "{:%Y-%m-%d %H:%M:%S}"
  },

  "hyprland/workspaces": {
    "format": "{icon}",
    "on-click": "hyprctl dispatch workspace {id}"
  },

  "cpu": {
    "format": "󰍛 {usage}%",
    "tooltip": false
  },

  "memory": {
    "format": "󰾆 {}%",
    "tooltip": true
  },

  "temperature": {
    "format": "󰔄 {temperatureC}°C",
    "tooltip": false,
    "critical-threshold": 75,
    "format-critical": "󰔄 {temperatureC}°C"
  },

  "network": {
    "format-wifi": "󰖩 {essid}",
    "format-ethernet": "󰈁 {ipaddr}",
    "format-disconnected": "󰖪",
    "tooltip": true
  },

  "pulseaudio": {
    "format": "󰕾 {volume}%",
    "format-muted": "󰖁 Mute"
  },

  "battery": {
    "format": "{icon} {capacity}%",
    "format-icons": ["󰂎", "󰁺", "󰁼", "󰁾", "󰂀", "󰁹", "󰂅"],
    "tooltip": true
  },

  "tray": {
    "icon-size": 18,
    "spacing": 6
  }
}
